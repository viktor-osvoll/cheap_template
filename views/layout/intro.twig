
{% block intro %}
 <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
    <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
    <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
  </ol>
    {% for field in fields.flexible_content %}
  <div class="carousel-inner">
    <div class="carousel-item active">
        <img src="{{ Image(field.slider_image_one) }}" class="d-block w-100">   
    </div>
    <div class="carousel-item">
      <img src="{{ Image(field.slider_image_two) }}" class="d-block w-100">
    </div>
    <div class="carousel-item">
      <img src="{{ Image(field.slider_image_three) }}" class="d-block w-100" >
    </div>
      </div>
    {% endfor %}
 </div>
<hr>
{# {% for field in fields.flexible_content %}
         <h3 class="site-title col-8" style="font-family: italic; text-align: left;">{{ field.slider_text }}</h3>
          <img style="width: 100%; margin: 0; " class="header_picture" src="{{ Image(field.slider_image) }}" />
    {% endfor %} #}



{% endblock %}